plan_id: M0_ablation_slot_attn_interleave

runs:
  # -------------------------
  # Ablation: Axial attention (Var-attn -> Slot-attn per encoder layer)
  # -------------------------
  - id_template: "SUP-{dataset}.M0SAI.sd{seed}"
    entry: downstream
    overrides:
      - data={dataset}
      - model=M0
      - model.multislot.slot_attn.enabled=true
      - model.multislot.slot_attn.mode=interleave
      - train=downstream
      - train.mode=ft
      - train.freeze_epochs=0
      - train.epochs=15
      - train.batch_size=128
      - train.patience=0
      - train.ssl_ckpt_path=null
      - optim.lr=0.0004
      - optim.scheduler=cosine
      - optim.min_lr=0.000001
      - optim.t_max=30
      - metadata.enabled=false
      - runtime.device=cuda
      - runtime.seed={seed}
      - runtime.deterministic=true
      - train.val_flag=test
      - run.code=SUP-{dataset}
      - run.hparams_tag=m0-interleave-slot-attn
      - run.id={id}
    sweep:
      dataset: [ETTh1, Exchange]
      seed: [0]

ops: []
cmps: []
aggs: []

